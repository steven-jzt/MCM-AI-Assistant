---
name: mcm-ai-assistant
description: 数学建模竞赛全流程助手——审题拆解、模型选择与推导、Python 代码求解、出版级可视化、论文撰写（LaTeX/Word）。三角色流水线（建模手→编程手→论文手）+ 5 质量门 Subagent 质检 + 候选模型竞争机制 + 两级回退 + 可运行模型库（~3700 行）。当用户要求数学建模、建模竞赛（国赛/美赛/校赛/华数杯等）、拆题、建模、求解、出图或写建模论文时使用。
---

# MCM-AI-Assistant

数学建模竞赛 AI 全流程智能体：从审题到论文，一条龙辅助。本技能把竞赛拆为**三角色流水线**（建模手 → 编程手 → 论文手），每阶段有独立交付物和只读 Subagent 质量门；同时内置**候选模型竞争机制**、**两级回退机制**和**可运行模型库**。

## 根目录契约

- `SKILL_ROOT`：本技能安装目录，**只读**。含 `references/`（角色文档与质检）、`model_library/`（模型库）、`utils/`（工具）、`tools/`（工具链）、`assets/`（算法资产）、`template/`（论文模板）。
- `PROJECT_ROOT`：用户赛题所在目录，所有产物（`code/`、`data/`、`figures/`、`results/`）只写这里。
- 两个根目录必须不同；默认禁止覆盖 `SKILL_ROOT` 内任何文件。确需修改模板时，先复制到 `PROJECT_ROOT` 再处理。
- 输入附件只读。

## 强制执行协议

1. **阶段顺序执行**：严格按 建模手 → 编程手 → 论文手 顺序，禁止跳阶段或并行。每阶段内渐进加载文档，禁止一次性读取全部参考。**例外**：质量门触发回退时允许退回上一阶段或本阶段开头。
2. **文件保护**：禁止覆盖题目原始附件、`SKILL_ROOT` 内的 `model_library/` 源码和 `template/` 模板文件。所有输出写入 `PROJECT_ROOT` 的 `code/`、`figures/`、`results/` 目录。
3. **质量门强制**：每阶段结束派发对应的只读独立 Subagent（见 `.claude/agents/` 与 `references/Subagent调度.md`）执行核验（M1/P1/P2/W1/W2），返回 PASS/FAIL/BLOCKED 证据回执。禁止等全流程结束才首次派发。
4. **Subagent 默认策略**：默认只派发 5 个固定质量门 Subagent（只读，不修改文件）。可选协作型 Subagent（文献调研、算法原型、对照实现等）默认关闭，仅在用户明确启用时派发。
5. **模型数量限制**：每个子问题最多 2 个独立模型；同一机制的多个变体按 1 个模型族计数。
6. **官方规则优先**：mcm.edu.cn（CUMCM）与 COMAP（MCM/ICM）官方要求优先级最高。本技能质量目标与官方硬约束冲突时（如 30 页正文上限），让位于官方硬约束。
7. **模板冲突由参赛场景决定**：校赛/华数杯/国赛/美赛模板格式要求不一致时（页边距、字体、页数上限、是否含封面），必须按当前参赛竞赛类型决定优先级，禁止在未确认参赛场景时擅自选定。论文手阶段一开始就确认并记录竞赛类型。
8. **可复现性强制**：所有数值结果附带随机种子（seed=42）、依赖版本快照和复现命令；代码从零运行即可复现全部结果。
9. **渐进加载**：每阶段只读该阶段必读文档，按需加载可选，避免上下文污染。
10. **AI 使用台账**：每阶段结束填写 `references/AI使用台账模板.md`，记录工具、使用方式、采纳情况和人工修改；W2 前由论文手汇入论文的 AI 使用声明。

## 建模通用提醒

1. **探索数据再建模**：检查分布形态、缺失值、异常值、结构性断点，理解数据生成过程后再选方法。
2. **从简单到复杂**：简单方法能解决的不用复杂方法；用了复杂方法就同时跑一个简单版本对照。
3. **验证多维度**：不同方法给同一答案，比同一方法在不同输入上表现一致更有说服力。
4. **结果诚实**：报告局限性和误差范围，不只展示最好的那组数字。

## 路由

| 用户意图 | 入口文档 | 前置条件 |
|---|---|---|
| 完整建模、完成整题 | `references/roles/建模手/SKILL.md` → `references/roles/编程手/SKILL.md` → `references/roles/论文手/SKILL.md` | 按顺序执行 |
| 只做题目分析、选模型 | `references/roles/建模手/SKILL.md` | 否 |
| 只写代码、跑结果、出图 | `references/roles/编程手/SKILL.md` | 需要题目和可执行的模型说明；缺失时先补齐必要分析 |
| 只写或修改论文 | `references/roles/论文手/SKILL.md` | 需要题目、模型、真实运行结果和图表；缺失时回退到对应阶段 |

不要在单阶段任务中强制执行完整流程。

## 渐进式加载

先读当前阶段的角色 `SKILL.md`，再按其中「何时加载」表读取所需参考，禁止一次性加载全部资料。

| 当前阶段 | 必读 | 按需加载 |
|---|---|---|
| 建模手 | `references/roles/建模手/SKILL.md`、`references/roles/建模手/references/工作流程.md` | 常见模式、`assets/*`、`assets/方法选择决策树.md` |
| 编程手 | `references/roles/编程手/SKILL.md`、`references/roles/编程手/references/工作流程.md` | 常见模式、可视化规范、图表选择与避坑 |
| 论文手 | `references/roles/论文手/SKILL.md`、`references/roles/论文手/references/工作流程.md` | 章节模板、写作规范、英文化工作流 |
| 选模型或查算法 | `assets/README.md` → 具体 `assets/0X-*.md` | — |
| 搜索论文 | `tools/paper_search/SKILL.md` | — |
| 生成 Word 论文 | `tools/docx/SKILL.md` | — |
| 生成 LaTeX 论文 | `tools/latex/SKILL.md` | — |
| 派发 Subagent 或阶段质检 | `.claude/agents/` + `references/Subagent调度.md` | — |

## 质量门机制

每阶段结束派发只读独立 Subagent 核验，门控 Subagent 必须返回固定格式回执（范围 / 输入快照 / 状态 PASS|FAIL|BLOCKED / 证据 / 发现 / 返工建议）。原生定义见 `.claude/agents/{m1,p1,p2,w1,w2}-gate.md`，协议细节与反馈回路见 `references/Subagent调度.md`。

| 门 | 时机 | 关注点 | 解除条件 |
|---|---|---|---|
| M1 | 建模手交付前 | 结论-模型匹配、证据、模型数量、**数据探索完成度** | 每个子问题≤2 独立模型，术语一致，结论可检验，数据已做全貌探索并记录处理决策 |
| P1 | 编程手全量计算前 | 最小可运行结果、**数据筛选合理性**、**竞争淘汰结果** | 输入输出链路跑通，结果可复现，数据筛选有领域理由，竞争对比表产出，至少一个候选模型通过淘汰 |
| P2 | 编程手交付前 | 代码、图、表格、复现清单、**结果可靠性与稳健性分析** | ≥9 张图（PNG+SVG），figure_audit 通过，复现清单完整，结果含误差评估或交叉验证，关键参数敏感性扫描完成 |
| W1 | 论文手长正文前 | 证据大纲 | 每个结论都有可追溯证据 |
| W2 | 论文手交付前 | **第一层** paper_audit.py 自动脚本 → **第二层** 全文一致性、AI 台账完整性 | paper_audit 通过，公式/图表/文献/格式全部校验通过，AI 台账已完整填写 |

## 竞争机制

建模手为每个子问题推荐 2-3 个候选模型后，不在纸面分析中指定最终选择；候选全部进入 **P1 最小可运行结果阶段**，在同一小数据集上实际跑通，用定量指标决出优胜者。

- **候选池建立（建模手出）**：为每个子问题列出 2-3 个候选模型，标注复杂度、数据需求和预期适用场景；其中一个必须是简单基准模型（贪心、朴素预测、固定规则等）。
- **P1 竞争淘汰（编程手执行）**：在同一 mini-batch 数据集上运行所有候选，产出**竞争对比表**（核心指标值 / 运行时间 / 数据假设满足度 / 保留结论）。
- **淘汰规则**：
  - 复杂模型在所有指标上均不如简单基准 → 淘汰复杂模型；
  - 两个候选指标接近（差距 < 10%）→ 两者都保留，全量计算后作为论文模型对比素材；
  - 候选结果出现物理意义错误 → 立即淘汰。
- **竞争不计入模型数量限制**：淘汰掉的不计入「每子问题 ≤2 个独立模型」配额，只有保留进入全量计算的才计入。

竞争原则：数据说了算，不做纸面优胜者；承认简单模型有时就是最好的模型；竞争对比表本身就是论文的「模型选择依据」素材。

## 回退机制

质量门核验不仅判定 PASS/FAIL，还决定 FAIL 时的返工深度，回退分两级：

| 级别 | 回退深度 | 触发条件 | 操作 |
|---|---|---|---|
| **一阶回退** | 同阶段内修正 | 参数/数据处理/代码实现有问题，但模型选择和方案设计合理 | 在当前阶段内修正，不改模型选择，修改后重新通过同一质量门 |
| **二阶回退** | 退回上一阶段重新设计 | 模型假设与数据严重不匹配、结果物理意义错误、连续两次一阶返工仍未 PASS | 丢弃不可复用部分，退回上一阶段重新选方案，重走后续流程 |

各质量门的二阶回退触发条件见 `references/Subagent调度.md` 与对应 `*.md` 门定义。

回退原则：保留可复用资产（标记 `_archived/` 或 `_deprecated` 后缀）；在 `results/` 追加回退记录（原因、触发门、退回何处、替代方案）；允许二阶回退后复用已验证的代码框架与可视化模板，不从零起步。

## 模型速查

| 问题场景 | 首选模型 | 代码位置 |
|---|---|---|
| 综合评价 | 熵权法 + TOPSIS / AHP / 模糊评价 | `model_library/evaluation.py` |
| 预测问题 | GM(1,1) / ARIMA / LSTM（大数据） | `model_library/prediction.py` |
| 优化问题 | 线性规划 / 整数规划 / 遗传算法 | `model_library/optimization.py` |
| 微分方程 | ODE 数值解 / PDE 有限差分 | `model_library/differential.py` |
| 统计分析 | 多元回归 / PCA / K-means / 层次聚类 | `model_library/statistics.py` |
| 图论与网络 | 最短路径 / 网络流 / 关键路径 | `assets/04-*.md` |
| 综合类 | 蒙特卡洛 / 排队论 / 博弈论 / 元胞自动机 | `assets/06-*.md` |
| 机器学习 | 随机森林 / AdaBoost / Isolation Forest | `assets/07-*.md` |

解题时优先调用 `model_library/` 现有函数或在此基础上调整修改。详细方法选择见 `assets/方法选择决策树.md`（Domain → Subdomain → Method）。拟合模型必须输出训练/测试误差对比并给出过拟合风险评估。

## 输出规范

- 数学符号与公式一律 LaTeX（行内 `$...$`，独立 `$$...$$`）。
- 代码统一 Python 3，含必要的库导入、注释和错误处理，设置 `random_seed=42`。
- 图表用 `matplotlib` 或 `seaborn`，中文字体 `SimHei` 或 `Songti SC`：
  - 同时保存 PNG（≥300 DPI）+ 可编辑 SVG 双格式；
  - 色盲友好调色板（viridis/magma/cividis），禁用彩虹色阶（jet/rainbow）；
  - 出版级样式：白底、无上/右脊线、无网格、7-9pt 标签字体；
  - 保存前执行 `references/roles/编程手/scripts/figure_audit.py` 审计，灰阶预览至 `_qa/`。
- 论文文字学术风格，客观、简洁，避免口语化。

## 完成判定

- 声称完整完成时，当前任务涉及的质量门均为 PASS，且通过后产物未发生未经复验的实质变化；缺少 Subagent 能力时只能报告 BLOCKED 或受限交付。
- 所有计算结论来自实际运行结果；公式、表格、图表与代码结果一致；引用可由 OpenAlex/Crossref 追溯。
- 论文已按目标竞赛官方规则配置，篇幅目标已确认，图表编号与正文引用连续，参考文献与正文引用双向对应。
- 每次回答结尾提供三个清单：**已完成** / **待完成** / **需要我确认**。
