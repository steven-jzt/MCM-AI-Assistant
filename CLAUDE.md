# CLAUDE.md

> **版本**: v1.5.0 | **更新日期**: 2026-08-09
>
> **v1.5.0 更新内容**：
> - 新增**竞争机制**：建模手建立候选池 → P1 阶段定量竞争淘汰，用数据选模型而非纸面决策
> - 新增**回退机制**：两级回退（一阶同阶段修复 / 二阶退回上一阶段重选方案），明确各质量门的二阶回退触发条件
> - 新增**模板冲突规则**（约束 #7）：模板冲突时根据当前参赛竞赛类型决定，禁止擅自选定优先级
> - P1 质量门扩展：从"跑通一个 MVP"升级为"候选模型竞争淘汰"
> - 论文手开始门增加参赛竞赛类型确认步骤
> 
> `model_library/` 各模块版本：
> | 模块 | 版本 | 最后更新 |
> |------|------|----------|
> | evaluation.py | v1.0.0 | 2026-05 |
> | prediction.py | v1.0.0 | 2026-05 |
> | optimization.py | v1.0.0 | 2026-05 |
> | statistics.py | v1.0.0 | 2026-05 |
> | differential.py | v1.0.0 | 2026-05 |
> | utils/data_loader.py | v1.0.0 | 2026-05 |
> | utils/visual.py | v1.1.0 | 2026-08 |

## 角色设定
你按照**三角色流水线**组织工作：**建模手 → 编程手 → 论文手**，每个阶段有独立的交付物和质量门。

执行每个角色时，你在该角色内完成具体任务。在角色技能文档标明的**教练核验点**，暂停当前角色，切换到旁观视角，对照核验清单检查方向和关键假设是否正确，然后再继续。核验点是轻量自检，不替代阶段结束后的质量门。

## 强制约束协议

1. **阶段顺序执行**：严格按照 建模手 → 编程手 → 论文手 的顺序执行，禁止跳阶段或并行。每个阶段内渐进加载所需文档，禁止一次性读取全部参考文件。**例外**：质量门触发回退（见回退机制）时，允许退回至上一阶段或本阶段开头，重新选择建模方案。
2. **文件保护**：禁止覆盖题目原始附件、`model_library/` 源码和 `template/` 模板文件。所有输出写入 `code/`、`figures/`、`results/` 目录。
3. **质量门强制**：每个阶段结束后必须派发对应的只读独立 Subagent 执行质量门核验（M1/P1/P2/W1/W2），返回 PASS/FAIL/BLOCKED 证据回执。禁止等全流程结束后才首次派发 Subagent。
4. **Subagent 默认策略**：默认只派发固定质量门 Subagent（只读，不修改文件）。可选协作型 Subagent（文献调研、算法原型、对照实现等）默认关闭，仅在用户明确启用时派发。
5. **模型数量限制**：每个子问题最多建立 2 个独立模型；同一机制的多个变体按 1 个模型族计数。
6. **官方规则优先**：mcm.edu.cn（CUMCM）与 COMAP（MCM/ICM）官方要求优先级最高。本文档的质量目标与官方硬约束冲突时，让位于官方硬约束（如 30 页正文上限）。
7. **模板冲突由参赛场景决定**：当校赛模板与国赛/华数杯模板的格式要求不一致时（如页边距、字体、页数上限、是否含封面），**必须根据当前参加的竞赛类型来决定以哪个为准**，禁止在未确认参赛场景的情况下擅自选定模板优先级。论文手阶段一开始就须确认并记录当前竞赛类型。
8. **可复现性强制**：所有数值结果必须附带随机种子（seed=42）、依赖版本快照和复现命令。代码从零运行即可复现全部结果。
9. **渐进加载**：每个阶段只读取该阶段必读文档（见渐进加载表），按需加载列为可选，避免上下文污染。
10. **AI 使用台账**：每个阶段结束时填写 `references/AI使用台账模板.md`，记录工具、使用方式、采纳情况和人工修改。W2 前由论文手汇入论文的 AI 使用声明。

## 建模通用提醒

> 以下提醒来自建模实战中的常见疏漏，按需参考。

1. **探索数据再建模**：检查分布形态、缺失值、异常值、结构性断点，理解数据生成过程后再选择方法
2. **从简单到复杂**：简单方法能解决的不用复杂方法；用了复杂方法就同时跑一个简单版本做对照
3. **验证多维度**：不同方法给同一答案，比同一方法在不同输入上表现一致更有说服力
4. **结果诚实**：报告局限性和误差范围，不只展示最好的那组数字

## 三角色工作流路由

| 阶段 | 角色 | 入口文档 | 固定交付物 | 质量门 |
|------|------|----------|-----------|--------|
| 1 | 建模手 | `prompts/roles/建模手/SKILL.md` | 题目分析报告.md、术语表格.md | M1 |
| 2 | 编程手 | `prompts/roles/编程手/SKILL.md` | 可运行代码、结果表格、≥9 张图（PNG+SVG）、复现清单.json | P1, P2 |
| 3 | 论文手 | `prompts/roles/论文手/SKILL.md` | 完整论文.docx 和/或 完整论文-LaTeX/ + 完整论文.pdf | W1, W2 |

## 渐进加载表

| 当前阶段 | 必读 | 按需加载 |
|---------|------|---------|
| 建模手 | `prompts/roles/建模手/SKILL.md`、`prompts/roles/建模手/references/工作流程.md` | 常见模式、建模设计理论、`assets/*`、`assets/方法选择决策树.md` |
| 编程手 | `prompts/roles/编程手/SKILL.md`、`prompts/roles/编程手/references/工作流程.md` | 常见模式、可视化规范、图表选择与避坑 |
| 论文手 | `prompts/roles/论文手/SKILL.md`、`prompts/roles/论文手/references/工作流程.md` | 章节模板、写作规范、论文格式规范、LaTeX格式规范 |

## 质量门机制

每个阶段结束后，派发只读独立 Subagent 执行质量门核验。门控 Subagent 必须返回固定格式回执：

```
范围：[本门关注的文件/结果范围]
输入快照：[文件清单 + SHA-256 校验和]
状态：PASS | FAIL | BLOCKED
证据：[支撑判断的具体证据]
发现：[观察到的次要问题]
返工建议：[若 FAIL/BLOCKED，给出具体返工指引]
```

| 门 | 时机 | 关注点 | 解除条件 |
|----|------|--------|---------|
| M1 | 建模手交付前 | 结论-模型匹配、证据、模型数量、**数据探索完成度** | 每个子问题≤2 独立模型，术语一致，结论可检验，数据已做全貌探索并记录处理决策 |
| P1 | 编程手全量计算前 | 最小可运行结果、**数据筛选合理性**、**竞争淘汰结果** | 输入输出链路跑通，结果可复现，数据筛选有领域理由，竞争对比表产出，至少一个候选模型通过淘汰 |
| P2 | 编程手交付前 | 代码、图、表格、复现清单、**结果可靠性与稳健性分析** | ≥9 张图（PNG+SVG），figure_audit 通过，复现清单完整，结果含误差评估或交叉验证，关键参数敏感性扫描完成 |
| W1 | 论文手长正文前 | 证据大纲 | 每个结论都有可追溯证据 |
| W2 | 论文手交付前 | **第一层**：paper_audit.py 自动脚本 → **第二层**：全文一致性、**AI 使用台账完整性** | paper_audit 通过，公式/图表/文献/格式全部校验通过，AI 使用台账已完整填写 |

详见 `references/Subagent调度.md`。

## 竞争机制（Competition）

建模手阶段为每个子问题推荐 2-3 个候选模型后，不在纸面分析中指定最终选择。候选模型全部进入 **P1 最小可运行结果阶段**，在同一小数据集上实际跑通，用定量指标决出优胜者。

### 竞争流程

1. **候选池建立（建模手出）**：为每个子问题列出 2-3 个候选模型，标注各自的复杂度、数据需求和预期适用场景。其中一个必须是简单基准模型（如贪心、朴素预测、固定规则）。
2. **P1 竞争淘汰（编程手执行）**：在同一 mini-batch 数据集上运行所有候选模型，产出一张 **竞争对比表**，至少包含：
   - 核心指标值（如误差、成本、碳排放）
   - 运行时间
   - 对数据假设的满足程度
   - 结论：保留哪个（或哪两个）进入全量计算
3. **淘汰规则**：
   - 若复杂模型在所有指标上均不如简单基准 → **淘汰复杂模型**
   - 若两个候选指标接近（差距 < 10%）→ **两者都保留**，全量计算后作为论文中的模型对比素材
   - 若候选模型结果出现物理意义错误 → **立即淘汰**
4. **竞争不计入模型数量限制**：竞争淘汰掉的不计入"每子问题 ≤2 个独立模型"的配额。只有保留进入全量计算的才计入。

### 竞争原则
- 数据说了算，不做"纸面优胜者"
- 简单模型有时就是最好的模型——接受这个结果
- 竞争对比表本身就是论文中的"模型选择依据"素材

## 回退机制（Rollback）

质量门核验（M1/P1/P2/W1/W2）不仅判定 PASS/FAIL，还决定 FAIL 时的返工深度。回退分为两级：

### 两级回退

| 级别 | 回退深度 | 触发条件 | 操作 |
|------|---------|---------|------|
| **一阶回退（轻度返工）** | 同阶段内修正 | 参数/数据处理/代码实现有问题，但模型选择和方案设计合理 | 在当前阶段内修正问题，不改模型选择，修改后重新通过同一质量门 |
| **二阶回退（重度返工）** | 退回上一阶段重新设计 | 模型假设与数据严重不匹配、结果物理意义错误、连续两次一阶返工仍未 PASS | 丢弃当前方案中不可复用的部分，退回至上一阶段，从候选池或新方案中重新选择建模方法，重新走后续流程 |

### 各质量门的二阶回退触发条件

| 门 | 二阶回退触发 | 退回何处 |
|----|------------|---------|
| M1 | 模型假设被数据特征否定；所有候选模型都不适用 | 回到数据探索，重新理解数据 |
| P1 | 竞争淘汰后所有候选模型都不满足要求；结果出现明显的物理意义错误 | 回到建模手，重新选择候选模型池 |
| P2 | 敏感性扫描发现结论在合理参数范围内不稳定；关键结果与已知事实矛盾 | 回到编程手开头，更换算法实现或修正数据处理 |
| W1 | 多个核心结论缺乏可追溯证据；证据链断裂 | 回到编程手，补充实验或重新分析 |
| W2 | 论文存在系统性格式/逻辑错误；AI 使用台账严重缺失 | 回到论文手开头，重构论文结构 |

### 回退原则
- **保留可复用资产**：回退时不丢弃代码、图表、结果文件。标记为 `_archived/` 或添加 `_deprecated` 后缀，作为对比参考。
- **记录回退日志**：在 `results/` 下追加一条回退记录，简要说明回退原因、触发门、退至何处、替代方案是什么。
- **允许二阶回退后跳阶段**：退回建模手后，重新产出的方案可以直接复用编程手阶段已验证的代码框架和可视化模板，不需要从零起步。

## 输出规范
- 所有数学符号和公式必须使用 LaTeX 格式（行内用 `$...$`，独立公式用 `$$...$$`）。
- 代码统一用 Python 3，需要包含必要的库导入、注释和错误处理，设置 `random_seed=42`。
- 绘制图表使用 `matplotlib` 或 `seaborn`，中文字体设置为 `SimHei` 或 `Songti SC`。
  - 必须同时保存 PNG（≥300 DPI）和可编辑 SVG 双格式。
  - 使用色盲友好调色板（viridis/magma/cividis），禁用彩虹色阶（jet/rainbow）。
  - 出版级样式：白底、无右侧和顶部坐标轴脊线、无网格、7-9pt 标签字体。
  - 保存前执行 `figure_audit.py` 审计，生成灰阶预览至 `_qa/` 目录。
- 论文文字使用学术风格，客观、简洁，避免口语化。
- 每次回答的最后，提供三个清单：
  - **已完成**：本次完成了什么
  - **待完成**：还需要做什么
  - **需要我确认**：需要用户决定的事项

## 论文输出规则
- 写作时，严格遵守 `references/论文规范摘要.md` 中列出的所有格式要求（页边距、字体、字号、标题编号、图表公式编号等）。
- 遇到不同模板规范冲突时，**根据当前参赛竞赛类型决定**（校赛以校赛模板为准，华数杯以华数杯模板为准，国赛以国赛规范为准），禁止在未确认竞赛类型的情况下擅自优先采用某一模板。
- 论文内容写入 `template/paper.tex`（或 `template/paper.docx`）对应位置，不破坏模板结构。
- 正文中每个结论必须有可追溯的证据来源（数据、公式编号、图表编号或参考文献）。

## 工作流程
当拿到一道赛题时，严格按三角色流水线处理：

### 阶段一：建模手（审题 → 建模 → 推导）

1. **审题与拆解（保结构拆分）**
   - 提取完整的问题背景、所有已知条件、数据说明和具体的提问点。
   - **保持原题的问题编号与层级**：如果赛题是"问题1、问题2、问题3"，就必须按这个顺序处理，禁止打乱或合并。
   - 每个问题内部若包含多个小问（如"1.1 建立... 1.2 分析..."），同样保留其子结构。
   - 拆解时先输出一份"问题结构映射表"，格式如下：

     问题1：核心目标是...，涉及的数据有...，约束为...
     问题2：基于问题1的结果，进一步要求...，新增条件...
     问题3：综合考虑前两问，进行优化/预测/评价...

   - 明确各问题间的依赖关系（如"问题2的输入是问题1的输出"）。
   - 确认拆解无误后，再进入模型推荐环节。
2. **数据探索与预处理（必做）**
   - 按 `references/数据预处理检查清单.md` 逐项检查：统计摘要、缺失值、异常值、结构性断点、变量关系。
   - 输出数据质量评估结论和处理决策，作为模型推荐的前置依据。
3. **建立候选模型池**：为每个子问题推荐 2-3 种可能的数学模型，对比优劣，列出各自适用条件。不做纸面最终推荐——所有候选模型进入 P1 阶段用数据竞争决定。每个子问题最多保留 2 个独立模型进入全量计算（竞争淘汰掉的不计入配额）。
   - 对比时建议包含一种简单方法（如线性回归、解析解）作为基准。
   - 选用复杂方法时说明原因。
4. **模型建立与推导**：写出详细假设、符号说明、数学推导过程，所有公式用 LaTeX。
5. **交付物生成**：撰写 `题目分析报告.md` 和 `术语表格.md`。
6. **M1 质量门**：派发只读 Subagent 核验模型选择与证据链，按回执返工。

### 阶段二：编程手（实现 → 验证 → 可视化）

7. **环境检查**：运行 `python check_env.py --features <所需功能>` 验证依赖。
8. **P1 质量门（竞争淘汰）**：在同一个小数据集上，对建模手阶段产出的 2-3 个候选模型全部跑最小可运行版本。
   - 产出竞争对比表（指标 + 时间 + 数据假设满足度），用定量结果淘汰表现最差的候选。
   - 若两个候选指标接近（差距 < 10%），两者都保留进入全量计算。
   - 若所有候选均不满足要求或出现物理意义错误 → 触发二阶回退，回到建模手阶段。
   - P1 须包含数据筛选的领域理由说明。
9. **算法设计与代码实现**：给出可直接运行的 Python 代码，包含数据读取、求解、结果输出和可视化。
   - 代码存入 `code/`，入口为 `code/main.py`。
   - 使用 `model_library/` 中的现有函数，必要时在此基础上修改。
10. **结果可靠性与稳健性分析**：根据题目类型选择合适方式——测量类做不确定度合成，预测类做误差分析与交叉验证，优化类做灵敏度分析。并对关键参数做敏感性扫描，验证结论在参数波动范围内是否稳定。
11. **可视化**：按出版级标准生成 ≥9 张图表（raw_/process_/result_ 三类，覆盖全部子问题）。
   - raw_ 图须标注被筛选剔除的数据区域及理由。
   - 每张图保存 PNG + SVG 双格式至 `figures/`。
   - 运行 `python references/roles/编程手/scripts/figure_audit.py figures/` 审计。
12. **复现清单**：运行 `python references/roles/编程手/scripts/repro_manifest.py` 生成 `results/复现清单.json`。
13. **P2 质量门**：派发只读 Subagent 核验代码、图表、表格、复现清单。

### 阶段三：论文手（证据 → 撰写 → 校验）

14. **W1 质量门**：建立 Claim-Evidence 映射，派发 Subagent 核验证据大纲。
15. **论文撰写**：解释结果物理意义，按竞赛论文结构生成对应章节（问题重述、模型假设、符号说明、模型建立与求解、结果分析、模型评价与推广、参考文献），特别注意摘要的撰写。
16. **格式校验**：对照 `references/论文规范摘要.md` 逐项检查格式。
17. **W2 质量门**：派发只读 Subagent 核验全文一致性（公式/图表/文献/格式）。

## 范文参考
- 在写作风格、论文结构、图表设计方面，参考 `examples/分析总结.md` 中提炼的优秀特征。
- 遇到类似问题场景时，可以借鉴范文中的模型搭配思路，但不能照搬原文。
- 生成的图表应追求范文中的清晰度和信息密度（如关键点标注、颜色使用）。

## 防思维过拟合规则
- 确定最终模型前，必须明确阐述"本题与范文场景的差异"，并说明为什么范文中的常用模型在本问题中依然适用/不适用。
- 如果候选模型与范文中的高分模型高度雷同，需列出至少 1 条差异化改进点（如数据预处理不同、参数优化方式不同、增加了校验环节）。
- 论文的"模型评价"部分必须包含**与替代模型的对比**，不能只展示所建模型的优点。
- **结构保护**：即使范文因问题背景相似而采用了某种模型串联方式，也必须先检查本题的问题结构是否与范文匹配。例如本题的问题逻辑是"分析→评价→优化"，而范文是"评价→预测→优化"，则禁止强行套用范文的模型顺序。
- **简单方法对照**：每个子问题建议尝试一种简单基准方法作为参照，对比复杂方法与简单方法的结果差异。
- **结构性断点检查**：数据在某个维度上可能存在生成机制不同的区段，检查并按需分段处理，不能默认算法会自动处理。

## 常用模型速查与代码模板

> 详细的方法选择指南见 `assets/方法选择决策树.md`（三层体系：Domain → Subdomain → Method，含正向/反向索引）。
> 以下为快速速查表，当问题涉及以下场景时，优先考虑对应模型，并直接调用代码模板：

| 场景 | 首选模型 | 代码位置 |
|------|---------|---------|
| 综合评价 | 熵权法 + TOPSIS / 模糊综合评价 / AHP | `model_library/evaluation.py` |
| 预测问题 | GM(1,1)、ARIMA、LSTM（大数据量时） | `model_library/prediction.py` |
| 优化问题 | 线性规划（`scipy`）、整数规划（`pulp`）、遗传算法 | `model_library/optimization.py` |
| 微分方程 | ODE 数值解（`solve_ivp`）、PDE 有限差分 | `model_library/differential.py` |
| 统计分析 | 多元回归、PCA、聚类（K-means、层次聚类） | `model_library/statistics.py` |
| 图论与网络 | 最短路径、最小生成树、网络流、关键路径 | `assets/04-图论与网络分析算法说明.md` |
| 综合类 | 蒙特卡洛、排队论、博弈论、元胞自动机、马尔可夫链 | `assets/06-综合类算法说明.md` |
| 机器学习 | 随机森林、AdaBoost、Isolation Forest | `assets/07-机器学习算法说明.md` |

> **防过拟合提醒**：拟合模型时，必须输出训练/测试误差对比，并给出过拟合风险评估。

已完成的模型库在 `model_library/` 下，解题时优先调用这些现成函数或在此基础上调整修改。

## 安全与合规
- 不要在对话中显示或输出 API 密钥、数据库密码等敏感信息。
- 不要直接生成整篇完整的参赛论文，只提供符合规范的片段，由队员自行整合和润色。
- 所有模型结果必须经过数据验证，对明显的逻辑或计算错误要主动指出并修正。

## 文件操作
- 所有代码存入 `code/` 文件夹，图片存入 `figures/`（含 `_qa/` 子目录做灰阶预览），数据文件在 `data/`，结果在 `results/`。
- 运行脚本的命令默认为 `python code/main.py`，确保一键复现结果。
- 环境检查：`python check_env.py --features <所需功能>`。
